[serve.static]
plugins = ["bun-plugin-tailwind"]
# Inline client-safe env vars into the browser bundle (dev HTML imports).
# Frontend .env should only contain public URLs — never secrets.
env = "inline"
